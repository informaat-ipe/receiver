'use strict';

var expect = require('chai').expect;

describe('server', function() {
	var server = require('../sources/server.js');

	it('should return an express app');
	it('should expose the correct endpoint');
});
