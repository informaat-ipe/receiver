'use strict';

var expect = require('chai').expect;

describe('handler', function() {
	it('should take a response, pull it through `parser` and give the result to `sender`');
	it('should handle return codes');

	describe('error handling', function() {
		it('should throw an error when it receives an error');
	});
});
